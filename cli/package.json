{
  "name": "@policykit/cli",
  "version": "0.1.0",
  "description": "CLI tool for PolicyKit — manage decentralized policies for smart accounts",
  "license": "MIT",
  "type": "module",
  "bin": {
    "policykit": "./dist/index.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@policykit/sdk": "workspace:*",
    "commander": "^13.0.0",
    "viem": "^2.23.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
