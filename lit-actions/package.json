{
  "name": "@policykit/lit-actions",
  "version": "0.1.0",
  "description": "Lit Protocol v8 (Naga) Actions for PolicyKit off-chain policy evaluation",
  "license": "MIT",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "build",
    "src"
  ],
  "scripts": {
    "build": "node esbuild.config.js",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
